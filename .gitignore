node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example

# Tauri / Rust
src-tauri/target
src-tauri/gen

# Code-signing (chứa thumbprint / khóa riêng — không commit)
src-tauri/tauri.signing.json
*.pfx
*.p12

# Google service account key — không commit
google-sa.json
**/google-sa.json
mercurial-snow-*.json
*.iam.gserviceaccount.json

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Internal AI-assistant project memory — local only, not part of the public repo
CLAUDE.md
